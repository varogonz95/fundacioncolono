@component('components.forms.usuario')

@slot('email_model', 'update.usuario.email')
@slot('email_value', 'selected.usuario.email')

@slot('username_model', 'update.usuario.username')
@slot('username_value', 'selected.usuario.username')

@slot('password_model', 'update.usuario.password')
@slot('password_value', 'selected.usuario.password')

@endcomponent
